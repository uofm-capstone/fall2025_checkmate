<div class="container">
  <div class="d-flex bg-color1 text-white justify-content-center align-items-center m-5 p-3">
    <div class="d-flex flex-column">
      <h2 style = "text-align: center"><%= @sprint.name %></h2>
      <div>
        <p>
        <strong>Start date:</strong>
          <%= @sprint.start_date %>
        </p>
        <p>
        <strong>End date:</strong>
          <%= @sprint.end_date %>
        </p>
      </div>
      <%= link_to 'Back', semester_sprints_path(@semester), class: 'btn btn-outline-secondary' %>
    </div>
  </div>
</div>